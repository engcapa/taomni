# LAN Collaborative Whiteboard

## What & Why
在内网通讯核心之上增加**多人协作白板**,供局域网团队实时一起画图、标注、头脑风暴。画板操作通过核心任务建立的 P2P 通道在参与者之间同步,无需服务器,低延迟。可独立打开,也可在会议中作为协作画布使用。

**关键约束:** 绘制操作的同步走核心任务的节点间直连通道(操作广播 / CRDT 思路),保证多人并发编辑时的一致性。

## Done looks like
- 可对某成员或群组发起一块共享白板,受邀者加入后看到同一画面。
- 多人可同时绘制:画笔、形状、文字、橡皮擦等基础工具。
- 他人的绘制实时出现,并能看到对方光标位置。
- 多人同时编辑不会互相覆盖丢失,断线重连后画面能恢复一致。
- 白板可独立标签页打开,也可在会议中调出。
- Web 预览模式下给出"仅桌面版可用"的友好提示。

## Out of scope
- 文字消息、文件传输、音视频会议(其他任务)。
- 白板导出为图片/PDF、无限历史版本回放等高级功能(可作后续增强)。
- 跨网段/公网协作。

## Steps
1. **白板画布与工具** — 实现共享画布与基础绘图工具(画笔、形状、文字、橡皮擦),本地渲染流畅。
2. **操作同步** — 将绘制操作通过 P2P 通道广播给参与者,采用操作广播/CRDT 方式合并并发编辑,保证一致性。
3. **会话集成** — 在内网通讯界面发起/加入白板,支持独立标签页与会议内调出,显示多人光标。
4. **Web 预览桩** — 桩层对白板同步命令返回明确的"浏览器预览不支持"提示。

## Relevant files
- `src/types/index.ts`
- `src/stores/appStore.ts`
- `src/layouts/MainLayout.tsx`
- `src/lib/ipc.ts`
- `src/stubs/tauri-core.ts`
- `src-tauri/src/lib.rs`
