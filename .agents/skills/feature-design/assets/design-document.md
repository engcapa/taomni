# [功能名称] 详细设计

> 写作模板：替换所有方括号说明，按复杂度合并或删除不适用章节。保留必要未决项，但不要留下空表或模板提示。决策、验收、任务与验证 ID 在本设计内稳定。

## 1. 设计摘要与范围

- 类型：[新功能 / 现有能力扩展 / 功能设计细化]
- 文档位置：`docs-feature/<feature-slug>-design.md`
- 设计状态：[草案 / 部分可实施 / 可实施]
- 来源：[需求摘要、issue 或已有设计链接]
- 调研基线：[提交、日期、相关未提交变更及局限]
- 平台与运行方式：Windows、macOS、Linux 三端 Tauri 桌面应用，代码必须兼容三端构建与运行。
- 本轮真机执行端：[由当前环境识别；三端均保留计划，本轮完成当前端验证即可，其他端未执行不单独阻塞交付]
- 推荐方案：[关键行为变化、主要改动位置与选择理由]

[用一段话描述用户问题和完成后的可观察结果，写清本次范围、不做的相邻需求和兼容约束。设计状态不代表已经实现或验证。]

## 2. 当前实现与功能缺口

| 位置与符号 | 当前行为 / 契约 | 本次影响 | 依据与确定性 |
|---|---|---|---|
| [路径、符号] | [事实] | [复用、修改或新增原因] | [源码、日志、实测或推断] |

[描述现有入口到结果的调用链与数据归属；注明历史设计与当前源码不一致之处。]

## 3. 验收条件

| ID | 场景与前置条件 | 用户动作或触发 | 必须观察到的结果 | 适用平台 / 边界 |
|---|---|---|---|---|
| AC-01 | [条件] | [动作] | [行为、数据或系统结果] | [范围] |

[补充相关失败、恢复与兼容场景。性能有要求时给出负载、指标、测量边界及目标来源。]

## 4. 方案与关键决策

[说明推荐实现、为何解决问题、如何复用现有能力；仅对有实质取舍的决策列备选和成本。]

### 决策记录

| DEC ID / 问题 | 可行选项、影响与代价 | 结论或待决推荐及理由 | 状态 | 决策来源 | 关联 AC / TASK / V |
|---|---|---|---|---|---|
| DEC-01 [实质取舍] | [选项及体验、能力、成本差异] | [已定方案或明确标为待决的推荐] | [用户已定 / 授权代决 / agent 自决 / 待用户决策] | [用户答复摘要与轮次/日期、授权范围或代码依据] | [对应 ID，待决时注明影响] |

[记录影响方案的 UI/UX、组件选型或范围取舍，足以让下游理解为何这样选择；沿用的有效决定注明来源。待决项写明已向用户提出的具体问题、选项和受影响任务，不将推荐写成确认结论。收到答复后同步更新本表及方案、AC/TASK/V，修订保留 DEC ID 和变更原因。无需人工取舍时简述沿用依据，不保留空表；必要决策未定时设计保持草案或部分可实施。]

### 用户流程与交互（涉及 UI 时）

[入口、主流程、完成反馈、受影响文案和控件。对相关状态写清加载、空内容、错误、禁用、键盘焦点、退出与恢复行为。]

| 当前状态 | 动作或事件 | 前置条件 | 下一状态与可见反馈 | 失败 / 取消处理 |
|---|---|---|---|---|
| [状态] | [事件] | [条件] | [结果] | [处理] |

#### 原型与示意图（需要图稿决策时）

- 当前有效图稿：[预览相对链接、可编辑产物、方案/版本、关键状态及窗口尺寸；默认产物目录 docs-feature/<feature-slug>/]
- 图稿决策：[DEC ID、已向用户展示的版本与具体选择、用户反馈摘要或待反馈状态]
- 修订结果：[反馈采纳情况与理由、前后差异、最终选用或待决版本；实质变更关联后续决定]

[嵌入可查看的预览或提供可访问的原型链接。图稿、文字和 AC/TASK/V 一致；有旧版本时标清已替代。已有图稿决定或授权代决注明来源。未回复不能标用户已定，原型评审不能标成实现/测试通过。无需图稿时删除本小节。]

### 数据流、状态与生命周期

[权威状态位于何处，派生与持久化字段，读写时机，真实副作用；相关的任务、订阅、进程或句柄由谁创建和释放。并发、超时、重启、过期响应等仅保留本次相关内容。]

### 接口与共享契约

| 类型 / 名称（现有或拟新增） | 调用方 → 实现方 | 输入及序列化 | 输出 / 错误 / 事件 | 兼容规则 |
|---|---|---|---|---|
| [IPC、函数、数据类型或事件] | [位置与符号] | [字段、类型、默认/可空值] | [结构与语义] | [既有调用者影响] |

[复杂字段另列类型签名；有事件时写订阅/释放与顺序约束。无需变更接口时指出沿用的现有契约。]

### 三端兼容与相关存储、故障边界

[逐端说明受影响的依赖、条件编译、系统 API、路径、权限与 WebView 适配；平台特有能力的适配或降级应满足功能要求并保持三端可构建和运行。按需补充存储迁移、异常恢复和辅助测试的 browser stub 设计。]

## 5. 改动清单

| 路径 / 模块（标注拟新增） | 具体变更与保持的约束 | 相关 AC | 所属任务 |
|---|---|---|---|
| [路径] | [符号、算法或行为变更] | AC-01 | TASK-01 |

[包含相关 UI、store、IPC、Rust、stub、测试与必要配置；不要列无影响文件。]

## 6. 实现任务与交接

### TASK-01 [可交付目标]

- 职责与文件范围：[负责的路径 / 模块 / 共享文件]
- 输入与必读：[本设计小节、现有代码符号、相关规则或契约]
- 依赖：[前置 TASK、数据或接口契约、关联 DEC 及其状态；待决时注明等待条件，无则明确]
- 实施内容：[足够具体的改动步骤与关键约束]
- 对应验收：[AC ID]
- 验证与完成条件：[V ID、预期结果、应回填的产物]
- 并行与集成：[可独立进行的工作、共享改动协调方、整体集成责任]

[按需要重复任务块，明确最终集成、回归与真机验证由哪个任务承担。小功能可合并。]

## 7. 自动化测试计划

| V ID | AC / 用途（目标变化或保留行为） | 层级与文件 / case（标注拟新增） | 前置数据与操作 | 核心断言 | 命令、工作目录与前置依赖 | 改前依据 / 改后结果 |
|---|---|---|---|---|---|---|
| V-01 | AC-01 / [用途] | [Vitest/Rust/browser] | [输入与动作] | [可观察结果] | [经过核对的执行方式] | [已有基线或待执行检查] / 待执行 |

[UI 涉及时列 feature ID、case、controls/testid 的现有对应或拟新增映射及维护任务。指出各测试使用的 stub/mock/真实依赖和证明范围。列出受影响回归，说明哪些测试层不适用。]

[涉及既有行为时，明确受影响入口/共享消费者、保留的正常结果与恢复路径；高风险且缺测试的路径安排最小改前基线。对有意替换的 UI 标明期望变化，保留结果断言；不将旧测试失败后更新快照视为修复。本次引入的退化由对应 TASK 修复，目标验收和保留行为均满足后才能完成。]

## 8. 真机验证手册

### 环境与准备

[分别写清 Windows、macOS、Linux 的环境准备与验证步骤，标明当前运行端及本轮执行项。包括架构、WebView、被测构建、隔离 app-data/测试工作区、真实依赖、凭据环境变量名称及就绪检查；命令注明 shell 与目录。]

### V-02 [真实桌面验证目标]

- 对应验收：[AC ID]
- 执行前状态：[应用版本、数据、服务状态]
- 操作与逐步预期：[真实应用 UI 操作 → 可见结果 → 真实后端/系统副作用]
- 故障与恢复：[本次相关分支及预期]
- 证据：[命令、结果数据、日志、截图/录屏路径及来源标识]
- 清理：[本次测试资源的复原步骤]
- 状态：[待执行；缺少环境时写出缺口和接续方式]

[Windows、macOS、Linux 三端分别列验证项和状态，当前端完成后可满足本轮真机交付条件；其他端未执行标为未验证并保留后续步骤，不继承当前端结果，也不单独阻塞本轮交付。当前端自动化不可用时给可执行的手工真机步骤；涉及打包行为时加入打包应用路径。]

## 9. 验收追踪与交付条件

| AC | 方案位置 | 开发任务 | 验证项与平台 | 所需证据 / 实际证据链接 | 当前缺口 |
|---|---|---|---|---|---|
| AC-01 | [小节] | TASK-01 | V-01、V-02 | [标注待生成或实际产物] | [未实现、待执行或具体问题] |

[分别记录三端代码兼容检查、自动化及各端真机结果。本轮必要检查和当前端真机验证通过，且没有已知代码不兼容时，可以报告本轮完成；其他端未执行保留未验证和后续计划，不单独阻塞本轮交付。当前端的空产物、skip 或环境阻塞不能算验证完成，也不能从当前端通过声称三端均已实测通过。设计阶段没有执行的测试全部保持待执行。]

## 10. 风险、未决项与回退

| 项目 | 影响与依据 | 建议 / 缓解 / 最小验证 | 阻塞的具体任务 | 解除条件 |
|---|---|---|---|---|
| [确有必要的问题或风险] | [具体影响] | [推荐做法] | [TASK 或不阻塞] | [待确定决策或可取得证据] |

[涉及持久化或兼容性变化时说明代码回退与数据回退边界。最后明确现在可开始的任务与必须等待的任务；无未决项时直接写明，无需空表。]
