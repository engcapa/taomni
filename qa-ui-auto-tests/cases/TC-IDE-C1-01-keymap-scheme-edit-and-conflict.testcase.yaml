id: TC-IDE-C1-01
title: 'Keymap scheme surface: copy, draft record, Apply commit'
description: '§8.19.10 gate case C1 (browser portion) + ED-PARITY-004 A1.4/A1.5 draft/Apply
  boundary. Capability ids: editor.keymap-editable / keymap.scheme-surface.

  ED-PARITY-004 revision (P2): the previous description claimed a conflict but
  the steps never produced one, so the claim was unverifiable. This case now
  asserts the DEC-03 draft/Apply contract instead — conflict manufacture and
  both conflict exits live in TC-IDE-PARITY-004-02, which is the case that
  actually creates a contested chord.

  Required mode: browser (settings surface is pure renderer + per-profile
  localStorage). Highest claim on green: L1 browser-wired. Dispatches
  workspace.keymapSettings through Search Everywhere, copies a user scheme,
  records a fresh primary chord on editor.replace into the DRAFT, proves Apply
  is the only commit edge, then applies and closes.

  '
covers:
- F25.5
tags:
- code-workspace
- ide-c1
- keymap
- p1
modes:
- browser
fixtures:
- reset_db
timeout_sec: 150
steps:
- open: ${cfg.app.base_url}
- wait_for: '[data-testid="welcome-panel"]'
- click: '[data-testid="side-tab-tools"]'
- wait_for: '[data-testid="sidebar-tools-panel"]'
- click: '[data-testid="sidebar-tool-code-workspace"]'
- wait_for: '[data-testid="code-workspace-tab"]'
- click: '[data-testid="code-workspace-tree-add-folder"]'
- wait_for: '[data-testid="text-input-dialog"]'
- click: '[data-testid="text-input-dialog-confirm"]'
- wait_for: '[data-testid="code-workspace-tree-file"]'
- dblclick: '[data-testid="code-workspace-tree-file"]'
- wait_for: '[data-testid="code-workspace-editor-pane"]'
- click: '[data-testid="code-workspace-tab"]'
- wait: 0.3
- press: Control+Shift+N
- wait_for:
    selector: '[data-testid="code-workspace-search-everywhere"]'
    timeout_sec: 30
- click: '[data-testid="code-workspace-search-everywhere"] >> role=tab[name="Actions"]'
- click: '[data-testid="code-workspace-search-everywhere"] input[aria-label="Search actions"]'
- type: Keymap Settings
- click: '[data-testid="code-workspace-search-everywhere"] >> text=Keymap Settings'
- wait_for: '[data-testid="workspace-keymap-settings-dialog"]'
- assert_visible: '[data-testid="keymap-scheme-select"]'
- select_option:
    selector: '[data-testid="keymap-scheme-select"]'
    label: IDEA defaults (default)
- assert_attribute:
    selector: '[data-testid="keymap-scheme-select"]'
    name: value
    equals: ''
- assert_disabled: '[data-testid="keymap-settings-apply"]'
- click: '[data-testid="keymap-scheme-copy"]'
- eval_readonly:
    expression: document.querySelector("[data-testid" + String.fromCharCode(61) + "\"keymap-scheme-select\"] option:checked")?.textContent?.includes("copy")
    expect_truthy: true
- assert_enabled: '[data-testid="keymap-settings-apply"]'
- fill:
    selector: '[data-testid="keymap-action-filter"]'
    value: Replace in File
- wait_for: '[data-testid="keymap-row-editor.replace"]'
- screenshot: tc-ide-c1-dialog.png
- click: '[data-testid="keymap-add-editor.replace"]'
- press: Alt+Shift+r
- press: Enter
- assert_visible: '[data-testid="keymap-replace-editor.replace-0"]'
- assert_pattern:
    selector: '[data-testid="keymap-replace-editor.replace-0"]'
    regex: '(Alt|Option)[+]Shift[+]R'
- screenshot: tc-ide-c1-recorded.png
- click: '[data-testid="keymap-settings-apply"]'
- assert_disabled: '[data-testid="keymap-settings-apply"]'
- click: '[data-testid="keymap-settings-close"]'
- wait_for:
    selector: '[data-testid="workspace-keymap-settings-dialog"]'
    state: detached
    timeout_sec: 5
verification:
  review: reviewed
  requirements:
  - id: R1
    requirement: 'Keymap scheme surface: copy, draft record, Apply commit：按步骤执行后，所有声明的可观察结果均满足。'
    actions:
    - 1
    - 3
    - 5
    - 7
    - 9
    - 11
    - 13
    - 15
    - 17
    - 18
    - 19
    - 20
    - 23
    - 25
    - 26
    - 28
    - 30
    - 32
    - 33
    - 34
    - 35
    - 36
    - 38
    - 39
    - 40
    checkpoints:
    - step: 2
      expectation: '{"wait_for": "[data-testid=\"welcome-panel\"]"}'
    - step: 4
      expectation: '{"wait_for": "[data-testid=\"sidebar-tools-panel\"]"}'
    - step: 6
      expectation: '{"wait_for": "[data-testid=\"code-workspace-tab\"]"}'
    - step: 8
      expectation: '{"wait_for": "[data-testid=\"text-input-dialog\"]"}'
    - step: 10
      expectation: '{"wait_for": "[data-testid=\"code-workspace-tree-file\"]"}'
    - step: 12
      expectation: '{"wait_for": "[data-testid=\"code-workspace-editor-pane\"]"}'
    - step: 16
      expectation: '{"wait_for": {"selector": "[data-testid=\"code-workspace-search-everywhere\"]",
        "timeout_sec": 30}}'
    - step: 21
      expectation: '{"wait_for": "[data-testid=\"workspace-keymap-settings-dialog\"]"}'
    - step: 22
      expectation: '{"assert_visible": "[data-testid=\"keymap-scheme-select\"]"}'
    - step: 24
      expectation: '{"assert_attribute": {"selector": "[data-testid=\"keymap-scheme-select\"]", "name": "value",
        "equals": ""}}'
    - step: 25
      expectation: '{"assert_disabled": "[data-testid=\"keymap-settings-apply\"]"}'
    - step: 27
      expectation: '{"eval_readonly": {"expression": "document.querySelector(\"[data-testid\" + String.fromCharCode(61)
        + \"\\\"keymap-scheme-select\\\"] option:checked\")?.textContent?.includes(\"copy\")", "expect_truthy":
        true}}'
    - step: 28
      expectation: '{"assert_enabled": "[data-testid=\"keymap-settings-apply\"]"}'
    - step: 30
      expectation: '{"wait_for": "[data-testid=\"keymap-row-editor.replace\"]"}'
    - step: 35
      expectation: '{"assert_visible": "[data-testid=\"keymap-replace-editor.replace-0\"]"}'
    - step: 36
      expectation: '{"assert_pattern": {"selector": "[data-testid=\"keymap-replace-editor.replace-0\"]", "regex":
        "(Alt|Option)[+]Shift[+]R"}}'
    - step: 39
      expectation: '{"assert_disabled": "[data-testid=\"keymap-settings-apply\"]"}'
    - step: 41
      expectation: '{"wait_for": {"selector": "[data-testid=\"workspace-keymap-settings-dialog\"]", "state": "detached",
        "timeout_sec": 5}}'
    results:
    - 25
    - 28
    - 35
    - 36
    - 39
    - 41
  visual:
  - check: Keymap 对话框的 scheme 行、动作行、swatch 与底部 OK/Cancel/Apply 动作栏无截断或互相遮挡；
      录制中的 conflict 区在 720px 宽度内完整可见。
    method: manual
    reason: 像素与字体/DPI 边界不能由 DOM 断言代替。
  native:
  - check: 仅在用例声明的执行模式中采集证据；browser 证据不替代 native，Linux 结果不外推到 Windows/macOS。
    method: external
    reason: 明确记录执行边界和未验证平台。
