id: TC-auto-F1-5-tab-drag-reorder
title: Tab drag-and-drop reorders tabs in the tab bar
covers:
- F1.5
tags:
- auto-generated
- smoke
- needs-review
- tabs
- drag-drop
modes:
- browser
fixtures:
- reset_db
steps:
- open: ${cfg.app.base_url}
- wait_for: '[data-testid="tab-bar"]'
- click: '[data-testid="new-local-terminal"]'
- wait_for: '[data-testid="terminal-pane"]'
- wait: 0.5
- click: '[data-testid="new-local-terminal"]'
- wait: 0.5
- assert_count:
    selector: '[data-testid="tab-item"]'
    min: 3
- drag_to:
    from: '[data-testid="tab-item"][data-tab-type="terminal"] >> nth=0'
    to: '[data-testid="tab-item"][data-tab-type="terminal"] >> nth=1'
- wait: 0.5
- assert_count:
    selector: '[data-testid="tab-item"]'
    min: 3
- screenshot: auto-F1-5-tab-drag-reorder.png
verification:
  review: reviewed
  requirements:
  - id: R1
    requirement: Tab drag-and-drop reorders tabs in the tab bar：按步骤执行后，所有声明的可观察结果均满足。
    actions:
    - 1
    - 3
    - 6
    - 9
    checkpoints:
    - step: 2
      expectation: '{"wait_for": "[data-testid=\"tab-bar\"]"}'
    - step: 4
      expectation: '{"wait_for": "[data-testid=\"terminal-pane\"]"}'
    - step: 8
      expectation: '{"assert_count": {"selector": "[data-testid=\"tab-item\"]", "min": 3}}'
    - step: 11
      expectation: '{"assert_count": {"selector": "[data-testid=\"tab-item\"]", "min": 3}}'
    results:
    - 11
  visual:
  - check: 检查截图中关键控件、状态反馈、文本和布局无截断或遮挡；截图需要人工评审。
    method: manual
    reason: 像素与原生窗口边界不能由 DOM 断言代替。
  native:
  - check: 仅在用例声明的执行模式中采集证据；browser 证据不替代 native，Linux 结果不外推到 Windows/macOS。
    method: external
    reason: 明确记录执行边界和未验证平台。
