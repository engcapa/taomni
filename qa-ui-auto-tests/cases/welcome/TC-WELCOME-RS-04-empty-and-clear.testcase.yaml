id: TC-WELCOME-RS-04-empty-and-clear
title: Empty restore entry is disabled, builds a candidate through real use, and clear removes it
description: 'No seed: the restore entry starts disabled/empty. Creating and opening an

  embedded-File session through the UI produces a real snapshot candidate;

  clearing it through the confirm dialog leaves the entry empty and the

  legacy recent list does not resurrect it.

  '
covers:
- F1.6
tags:
- auto-generated
- p1
- welcome
- needs-review
modes:
- browser
fixtures:
- reset_db
- welcome_recents
timeout_sec: 90
steps:
- open: ${cfg.app.base_url}
- wait_for: '[data-testid="welcome-panel"]'
- wait_for:
    selector: '[data-testid="welcome-restore-status"][data-state="empty"]'
    timeout_sec: 15
- assert_count:
    selector: '[data-testid="welcome-restore-last-session"]:disabled'
    equal: 1
- click: text="New session…"
- wait_for: '[data-testid="session-editor"]'
- click: '[data-testid="session-proto-file"]'
- click: '[data-testid="session-section-bookmark"]'
- wait_for: '[data-testid="bookmark-settings"]'
- fill:
    selector: '[data-testid="session-name"]'
    value: qa-welcome-rs04-file
- fill:
    selector: '[data-testid="session-file-target"]'
    value: /preview
- click: '[data-testid="session-save"]'
- wait_for: '[data-testid="session-tree-item"][data-session-name="qa-welcome-rs04-file"]'
- open_session:
    name: qa-welcome-rs04-file
    double_click: true
- wait_for:
    selector: '[data-testid="tab-item"][data-tab-type="file-browser"]'
    timeout_sec: 15
- click: '[data-testid="tab-item"][data-tab-type="welcome"]'
- wait_for:
    selector: '[data-testid="welcome-restore-status"][data-state="available"]'
    timeout_sec: 20
- click: '[data-testid="welcome-restore-clear"]'
- wait_for: '[data-testid="confirm-dialog"]'
- click: '[data-testid="confirm-dialog-confirm"]'
- wait_for:
    selector: '[data-testid="welcome-restore-status"][data-state="empty"]'
    timeout_sec: 15
- assert_count:
    selector: '[data-testid="welcome-restore-last-session"]:disabled'
    equal: 1
- screenshot: F1.6-welcome-restore-clear.png
verification:
  review: reviewed
  requirements:
  - id: R1
    requirement: Empty restore entry is disabled, builds a candidate through real use, and clear removes it：按步骤执行后，所有声明的可观察结果均满足。
    actions:
    - 1
    - 5
    - 7
    - 8
    - 10
    - 11
    - 12
    - 14
    - 16
    - 18
    - 20
    checkpoints:
    - step: 2
      expectation: '{"wait_for": "[data-testid=\"welcome-panel\"]"}'
    - step: 3
      expectation: '{"wait_for": {"selector": "[data-testid=\"welcome-restore-status\"][data-state=\"empty\"]",
        "timeout_sec": 15}}'
    - step: 4
      expectation: '{"assert_count": {"selector": "[data-testid=\"welcome-restore-last-session\"]:disabled", "equal":
        1}}'
    - step: 6
      expectation: '{"wait_for": "[data-testid=\"session-editor\"]"}'
    - step: 9
      expectation: '{"wait_for": "[data-testid=\"bookmark-settings\"]"}'
    - step: 13
      expectation: '{"wait_for": "[data-testid=\"session-tree-item\"][data-session-name=\"qa-welcome-rs04-file\"]"}'
    - step: 15
      expectation: '{"wait_for": {"selector": "[data-testid=\"tab-item\"][data-tab-type=\"file-browser\"]", "timeout_sec":
        15}}'
    - step: 17
      expectation: '{"wait_for": {"selector": "[data-testid=\"welcome-restore-status\"][data-state=\"available\"]",
        "timeout_sec": 20}}'
    - step: 19
      expectation: '{"wait_for": "[data-testid=\"confirm-dialog\"]"}'
    - step: 21
      expectation: '{"wait_for": {"selector": "[data-testid=\"welcome-restore-status\"][data-state=\"empty\"]",
        "timeout_sec": 15}}'
    - step: 22
      expectation: '{"assert_count": {"selector": "[data-testid=\"welcome-restore-last-session\"]:disabled", "equal":
        1}}'
    results:
    - 21
    - 22
  visual:
  - check: 检查截图中关键控件、状态反馈、文本和布局无截断或遮挡；截图需要人工评审。
    method: manual
    reason: 像素与原生窗口边界不能由 DOM 断言代替。
  native:
  - check: 仅在用例声明的执行模式中采集证据；browser 证据不替代 native，Linux 结果不外推到 Windows/macOS。
    method: external
    reason: 明确记录执行边界和未验证平台。
