id: TC-WELCOME-RS-N-03-osc-cwd-record
title: 'Native: plain local terminal OSC 7 cwd records local-cwd directory use'
covers:
- F1.6
tags:
- welcome
- native-gate
- p1
- needs-review
modes:
- native
fixtures:
- reset_db
timeout_sec: 300
steps:
- open: ${cfg.app.base_url}
- vault_first_run: qa-native-gate-password
- wait_for: '[data-testid="welcome-panel"]'
- click: '[data-testid="welcome-open-local-terminal"]'
- wait_for:
    selector: '[data-testid="terminal-pane"]'
    timeout_sec: 30
- wait: 8
- click: '[data-testid="tab-item"][data-tab-type="welcome"]'
- wait_for: '[data-testid="welcome-panel"]'
- click: '[data-testid="welcome-history-tab-directories"]'
- wait_for: '[data-testid="welcome-local-directories"]'
- assert_visible: '[data-testid="welcome-local-directory"]'
- screenshot: F1.6-native-osc-cwd.png
verification:
  review: reviewed
  requirements:
  - id: R1
    requirement: 'Native: plain local terminal OSC 7 cwd records local-cwd directory use：按步骤执行后，所有声明的可观察结果均满足。'
    actions:
    - 1
    - 2
    - 4
    - 7
    - 9
    checkpoints:
    - step: 3
      expectation: '{"wait_for": "[data-testid=\"welcome-panel\"]"}'
    - step: 5
      expectation: '{"wait_for": {"selector": "[data-testid=\"terminal-pane\"]", "timeout_sec": 30}}'
    - step: 8
      expectation: '{"wait_for": "[data-testid=\"welcome-panel\"]"}'
    - step: 10
      expectation: '{"wait_for": "[data-testid=\"welcome-local-directories\"]"}'
    - step: 11
      expectation: '{"assert_visible": "[data-testid=\"welcome-local-directory\"]"}'
    results:
    - 10
    - 11
  visual:
  - check: 检查截图中关键控件、状态反馈、文本和布局无截断或遮挡；截图需要人工评审。
    method: manual
    reason: 像素与原生窗口边界不能由 DOM 断言代替。
  native:
  - check: 仅在用例声明的执行模式中采集证据；browser 证据不替代 native，Linux 结果不外推到 Windows/macOS。
    method: automated
    reason: 明确记录执行边界和未验证平台。
