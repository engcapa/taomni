id: TC-112
title: WSL session form mounts and saved session round-trips to proto=WSL
description: 'Coverage smoke for the new WSL feature. Opens SessionEditor, switches to

  the WSL protocol, asserts the WSL options form mounts (live argv preview

  visible), saves a session, and reopens it to confirm the proto=WSL chip

  is highlighted (round-trip fix in sessionTypeToProto).


  The test does NOT actually launch a wsl.exe terminal — browser-mode

  cannot spawn processes, and WSL is Windows-only at runtime. Distro

  discovery is gated on Windows, so on a non-Windows browser-mode runner

  the form falls through to the free-text distro input and the

  "unsupported" hint is asserted.

  '
covers:
- F9.8
- F6.3
tags:
- smoke
- p1
- wsl
- scaffolding-only
- no-launch
modes:
- browser
fixtures:
- reset_db
timeout_sec: 45
steps:
- open: ${cfg.app.base_url}
- wait_for: '[data-testid="sidebar"]'
- click: '[data-testid="session-new"]'
- wait_for: '[data-testid="session-editor"]'
- click: '[data-testid="session-proto-wsl"]'
- wait_for:
    selector: '[data-testid="wsl-argv-preview"]'
    timeout_sec: 5
- assert_visible: '[data-testid="wsl-argv-preview"]'
- assert_visible: '[data-testid="wsl-status-unsupported"]'
- fill:
    selector: '[data-testid="wsl-distro-text"]'
    value: qa-ubuntu-stub
- fill:
    selector: '[data-testid="wsl-user"]'
    value: ada
- fill:
    selector: '[data-testid="wsl-cwd"]'
    value: /home/ada
- fill:
    selector: '[data-testid="wsl-initial-cmd"]'
    value: echo qa-wsl
- click: '[data-testid="wsl-admin"]'
- click: '[data-testid="session-section-bookmark"]'
- wait_for: '[data-testid="bookmark-settings"]'
- fill:
    selector: '[data-testid="session-name"]'
    value: qa-ui-auto-wsl
- click: '[data-testid="session-save"]'
- wait_for: '[data-testid="session-tree-item"][data-session-name="qa-ui-auto-wsl"]'
- click: '[data-testid="session-tree-item"][data-session-name="qa-ui-auto-wsl"]'
- click: '[data-testid="session-edit"]'
- wait_for: '[data-testid="session-editor"]'
- wait_for:
    selector: '[data-testid="session-proto-wsl"][data-active="true"]'
    timeout_sec: 5
- assert_visible: '[data-testid="wsl-argv-preview"]'
- screenshot: 112-wsl-saved.png
verification:
  review: reviewed
  requirements:
  - id: R1
    requirement: WSL session form mounts and saved session round-trips to proto=WSL：按步骤执行后，所有声明的可观察结果均满足。
    actions:
    - 1
    - 3
    - 5
    - 9
    - 10
    - 11
    - 12
    - 13
    - 14
    - 16
    - 17
    - 19
    - 20
    checkpoints:
    - step: 2
      expectation: '{"wait_for": "[data-testid=\"sidebar\"]"}'
    - step: 4
      expectation: '{"wait_for": "[data-testid=\"session-editor\"]"}'
    - step: 6
      expectation: '{"wait_for": {"selector": "[data-testid=\"wsl-argv-preview\"]", "timeout_sec": 5}}'
    - step: 7
      expectation: '{"assert_visible": "[data-testid=\"wsl-argv-preview\"]"}'
    - step: 8
      expectation: '{"assert_visible": "[data-testid=\"wsl-status-unsupported\"]"}'
    - step: 15
      expectation: '{"wait_for": "[data-testid=\"bookmark-settings\"]"}'
    - step: 18
      expectation: '{"wait_for": "[data-testid=\"session-tree-item\"][data-session-name=\"qa-ui-auto-wsl\"]"}'
    - step: 21
      expectation: '{"wait_for": "[data-testid=\"session-editor\"]"}'
    - step: 22
      expectation: '{"wait_for": {"selector": "[data-testid=\"session-proto-wsl\"][data-active=\"true\"]", "timeout_sec":
        5}}'
    - step: 23
      expectation: '{"assert_visible": "[data-testid=\"wsl-argv-preview\"]"}'
    results:
    - 21
    - 22
    - 23
  visual:
  - check: 检查截图中关键控件、状态反馈、文本和布局无截断或遮挡；截图需要人工评审。
    method: manual
    reason: 像素与原生窗口边界不能由 DOM 断言代替。
  native:
  - check: 仅在用例声明的执行模式中采集证据；browser 证据不替代 native，Linux 结果不外推到 Windows/macOS。
    method: external
    reason: 明确记录执行边界和未验证平台。
