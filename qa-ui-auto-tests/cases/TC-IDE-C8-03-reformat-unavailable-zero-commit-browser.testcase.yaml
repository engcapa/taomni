id: TC-IDE-C8-03-reformat-unavailable-zero-commit-browser
title: Reformat unavailable provider keeps marker text untouched in browser
description: 'Browser gate case (ED-STYLE-001). Fills the preview README with a

  formatter-marker block plus badly indented code, fires Format Document

  (Ctrl+Alt+L) through the production keymap, and proves the typed

  unavailable path: the status bar reports no running formatter provider and

  the editor still holds the exact marker text and unformatted snippet

  (zero commit, marker regions never touched). Provider-backed commit/undo

  is covered by mounted unit tests and the live JDT LS provider fixture.

  '
covers:
- F25.5
tags:
- code-workspace
- ide-c8
- reformat
- browser-gate
- p1
modes:
- browser
fixtures:
- reset_db
timeout_sec: 180
steps:
- open: ${cfg.app.base_url}
- wait_for: '[data-testid="welcome-panel"]'
- click: '[data-testid="side-tab-tools"]'
- wait_for: '[data-testid="sidebar-tools-panel"]'
- click: '[data-testid="sidebar-tool-code-workspace"]'
- wait_for: '[data-testid="code-workspace-tab"]'
- click: '[data-testid="code-workspace-tree-add-folder"]'
- wait_for: '[data-testid="text-input-dialog"]'
- click: '[data-testid="text-input-dialog-confirm"]'
- wait_for: '[data-testid="code-workspace-tree-file"]'
- dblclick: '[data-testid="code-workspace-tree-file"]'
- wait_for: '[data-testid="code-workspace-editor-pane"]'
- click: '[data-testid="code-workspace-editor"] .cm-content'
- fill:
    selector: '[data-testid="code-workspace-editor"] .cm-content'
    value: "public class ReformatProbe {\n    // @formatter:off\n       badly_indented();\n    // @formatter:on\n\
      \        also_bad();\n}"
- press: Control+Alt+l
- assert_text:
    selector: '[data-testid="status-bar-message"]'
    contains: No formatter provider
    timeout_sec: 20
- assert_items:
    selector: '[data-testid="code-workspace-editor"] .cm-content .cm-line'
    equals:
    - public class ReformatProbe {
    - '    // @formatter:off'
    - '       badly_indented();'
    - '    // @formatter:on'
    - '        also_bad();'
    - '}'
- screenshot: tc-ide-c8-03-reformat-unavailable.png
verification:
  review: reviewed
  requirements:
  - id: R1
    requirement: 没有格式化提供者时快捷键明确报不可用，所有行及空白保持原样
    actions:
    - 14
    - 15
    checkpoints:
    - step: 16
      expectation: '{"assert_text": {"selector": "[data-testid=\"status-bar-message\"]", "contains": "No formatter
        provider", "timeout_sec": 20}}'
    - step: 17
      expectation: '{"assert_items": {"selector": "[data-testid=\"code-workspace-editor\"] .cm-content .cm-line",
        "equals": ["public class ReformatProbe {", "    // @formatter:off", "       badly_indented();", "    //
        @formatter:on", "        also_bad();", "}"]}}'
    results:
    - 16
    - 17
  visual:
  - check: 检查最终截图中的编辑器/菜单/状态反馈无遮挡和溢出；截图本身不是视觉通过。
    method: manual
    reason: 没有自动像素基准；需结合实际截图记录评审。
  native:
  - check: 真实 JDT LS 格式化由 TC-IDE-FMT-01-reformat-java-native 验证；此用例只证明无提供者分支。
    method: external
    reason: 按实际执行模式与指定边界解释证据。
