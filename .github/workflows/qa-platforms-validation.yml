name: QA platforms branch validation
on:
  push:
    branches: [feat/qa-ui-auto-platforms]
    paths: [.github/workflows/qa-platforms-validation.yml]
permissions:
  contents: read
  issues: write
jobs:
  smoke:
    # Temporary branch-only trigger for hosted validation (PTY and focused GTK input probes).
    uses: ./.github/workflows/qa-ui-auto-platforms.yml
    with:
      scope: selected
      case_ids: TC-001,TC-NATIVE-CORE-001,TC-012,TC-027,TC-auto-F-DB-1-query-tab-rename-native-restore,TC-IDE-C2-05-java-completion-refine-accept-native,TC-IDE-CW-EXECUTION-01-maven-run-build-tests-native,TC-IDE-IMPROVE-008-ime-lifecycle-native,TC-IDE-C8-02,TC-IDE-C3-02
  services:
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, windows-2025, macos-15]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    env:
      PYTHONPATH: .agents/skills/qa-ui-auto/scripts
      PYTHONUTF8: '1'
      PYTHONIOENCODING: utf-8
      PYTHONUNBUFFERED: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -r .agents/skills/qa-ui-auto/requirements-ci.txt
      - run: python .agents/skills/qa-ui-auto/scripts/ci_services.py install
      - run: python .agents/skills/qa-ui-auto/scripts/ci_services.py probe
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: service-probe-${{ matrix.os }}
          path: qa-ui-auto-report/service-probe
  java:
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, windows-2025, macos-15]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    env:
      PYTHONPATH: .agents/skills/qa-ui-auto/scripts
      PYTHONUTF8: '1'
      PYTHONIOENCODING: utf-8
      PYTHONUNBUFFERED: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install pyyaml jsonschema
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.1.0'
      - run: python .agents/skills/qa-ui-auto/scripts/ci_toolchains.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: java-probe-${{ matrix.os }}
          path: |
            qa-ui-auto-report/java-probe/readiness.json
            qa-ui-auto-report/java-probe/warmup.json
            qa-ui-auto-report/java-probe/jdtls-stderr.log
